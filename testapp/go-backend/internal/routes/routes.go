// AUTO-GENERATED BY VELD — DO NOT EDIT

package routes

import (
	"encoding/json"
	"net/http"

	"example.com/veld-generated/internal/interfaces"
	"github.com/go-chi/chi/v5"
)

// SetupRoutes registers all module routes with the given Chi router.
func SetupRoutes(r chi.Router, authSvc interfaces.AuthService, foodSvc interfaces.FoodService) {
	setupAuthRoutes(r, authSvc)
	setupfoodRoutes(r, foodSvc)
}

// writeJSON writes v as JSON with the given HTTP status code.
func writeJSON(w http.ResponseWriter, status int, v interface{}) {
	w.Header().Set("Content-Type", "application/json")
	w.WriteHeader(status)
	if v != nil {
		json.NewEncoder(w).Encode(v) //nolint:errcheck
	}
}

// readJSON decodes the request body into v.
func readJSON(r *http.Request, v interface{}) error {
	return json.NewDecoder(r.Body).Decode(v)
}

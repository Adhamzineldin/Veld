// AUTO-GENERATED BY VELD — DO NOT EDIT
// Zero-dependency hooks — no TanStack Query required.
import { useState, useEffect, useCallback } from 'react';
import { todosApi } from '../../client/todosApi';
import type { Todo, CreateTodoInput, UpdateTodoInput } from '../../types';

/** List all todos */
export function useListTodos() {
  const [data, setData] = useState<Todo[] | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    todosApi.listTodos()
      .then(setData)
      .catch(setError)
      .finally(() => setLoading(false));
  }, []);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/** Get a todo by ID */
export function useGetTodo(id: string) {
  const [data, setData] = useState<Todo | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);

  const fetch = useCallback(() => {
    setLoading(true);
    setError(null);
    todosApi.getTodo(id)
      .then(setData)
      .catch(setError)
      .finally(() => setLoading(false));
  }, [id]);

  useEffect(() => { fetch(); }, [fetch]);

  return { data, loading, error, refetch: fetch };
}

/** Create a new todo */
export function useCreateTodo() {
  const [data, setData] = useState<Todo | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { input: CreateTodoInput }) => {
    setLoading(true);
    setError(null);
    return todosApi.createTodo(vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => setLoading(false));
  }, []);

  return { mutate, data, loading, error };
}

/** Update a todo */
export function useUpdateTodo() {
  const [data, setData] = useState<Todo | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { id: string; input: UpdateTodoInput }) => {
    setLoading(true);
    setError(null);
    return todosApi.updateTodo(vars.id, vars.input)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => setLoading(false));
  }, []);

  return { mutate, data, loading, error };
}

/** Delete a todo */
export function useDeleteTodo() {
  const [data, setData] = useState<void | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<Error | null>(null);

  const mutate = useCallback((vars: { id: string }) => {
    setLoading(true);
    setError(null);
    return todosApi.deleteTodo(vars.id)
      .then((result) => { setData(result); return result; })
      .catch((err) => { setError(err); throw err; })
      .finally(() => setLoading(false));
  }, []);

  return { mutate, data, loading, error };
}

export const todosHooks = {
  useListTodos,
  useGetTodo,
  useCreateTodo,
  useUpdateTodo,
  useDeleteTodo,
};

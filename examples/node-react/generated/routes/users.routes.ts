// AUTO-GENERATED BY VELD — DO NOT EDIT
import type { IUsersService } from '../interfaces/IUsersService';
import { ApiError } from '../errors/users.errors';
import type { IUsersMiddleware } from '../middleware/IUsersMiddleware';

export function usersRouter(router: any, service: IUsersService, middleware: IUsersMiddleware): void {

  // List all users

  router.get('/api/users/', async (req: any, res: any) => {
    try {
      const result = await service.listUsers();
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // Get a user by ID

  router.get('/api/users/:id', async (req: any, res: any) => {
    try {
      const result = await service.getUser(req.params.id);
      res.json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // Create a new user

  router.post('/api/users/', middleware.validateCreateUserInput, async (req: any, res: any) => {
    try {
      const result = await service.createUser(req.body);
      res.status(201).json(result);
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });

  // Delete a user

  router.delete('/api/users/:id', async (req: any, res: any) => {
    try {
      await service.deleteUser(req.params.id);
      res.status(204).end();
    } catch (err: any) {
      const status = err.status ?? err.statusCode ?? 500;
      const body: Record<string, string> = { error: err.message ?? 'Internal Server Error' };
      if (err.code) body.code = err.code;
      res.status(status).json(body);
    }
  });
}

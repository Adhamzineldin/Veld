// AUTO-GENERATED BY VELD — DO NOT EDIT
import type { ITodosService } from '../interfaces/ITodosService';
import { CreateTodoInputSchema, UpdateTodoInputSchema } from '../schemas/schemas';

export function todosRouter(router: any, service: ITodosService): void {

  // List all todos

  router.get('/todos/', async (req: any, res: any) => {
    try {
      const result = await service.ListTodos();
      res.json(result);
    } catch (err: any) {
      if (err.name === 'ZodError') {
        return res.status(400).json({ error: 'Validation failed', details: err.errors });
      }
      const status = err.status ?? err.statusCode ?? 500;
      res.status(status).json({ error: err.message ?? 'Internal Server Error' });
    }
  });

  // Get a todo by ID

  router.get('/todos/:id', async (req: any, res: any) => {
    try {
      const result = await service.GetTodo(req.params.id);
      res.json(result);
    } catch (err: any) {
      if (err.name === 'ZodError') {
        return res.status(400).json({ error: 'Validation failed', details: err.errors });
      }
      const status = err.status ?? err.statusCode ?? 500;
      res.status(status).json({ error: err.message ?? 'Internal Server Error' });
    }
  });

  // Create a new todo

  router.post('/todos/', async (req: any, res: any) => {
    try {
      const input = CreateTodoInputSchema.parse(req.body);
      const result = await service.CreateTodo(input);
      res.status(201).json(result);
    } catch (err: any) {
      if (err.name === 'ZodError') {
        return res.status(400).json({ error: 'Validation failed', details: err.errors });
      }
      const status = err.status ?? err.statusCode ?? 500;
      res.status(status).json({ error: err.message ?? 'Internal Server Error' });
    }
  });

  // Update a todo

  router.put('/todos/:id', async (req: any, res: any) => {
    try {
      const input = UpdateTodoInputSchema.parse(req.body);
      const result = await service.UpdateTodo(req.params.id, input);
      res.json(result);
    } catch (err: any) {
      if (err.name === 'ZodError') {
        return res.status(400).json({ error: 'Validation failed', details: err.errors });
      }
      const status = err.status ?? err.statusCode ?? 500;
      res.status(status).json({ error: err.message ?? 'Internal Server Error' });
    }
  });

  // Delete a todo

  router.delete('/todos/:id', async (req: any, res: any) => {
    try {
      await service.DeleteTodo(req.params.id);
      res.status(204).end();
    } catch (err: any) {
      if (err.name === 'ZodError') {
        return res.status(400).json({ error: 'Validation failed', details: err.errors });
      }
      const status = err.status ?? err.statusCode ?? 500;
      res.status(status).json({ error: err.message ?? 'Internal Server Error' });
    }
  });
}
